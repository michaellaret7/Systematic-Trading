name: Ruff

on:
  push:
    branches: [dev]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Ruff check
        uses: astral-sh/ruff-action@v3
        with:
          args: check src scripts tests --extend-exclude ".*"
